# Local development defaults only. Generate unique production credentials.
POSTGRES_USER=admin
POSTGRES_PASSWORD=change-me-local-postgres
POSTGRES_DB=postgres

# Shared by the Go service configuration loader.
DATABASE_USER=admin
DATABASE_PASSWORD=change-me-local-postgres
